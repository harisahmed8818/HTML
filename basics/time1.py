import time
import os

Timespan = time.strftime('%H : %M : %S')
print(Timespan)

Timespan = time.strftime("%H")
print(f"Hours : {Timespan}")

Timespan = time.strftime("%M")
print(f"Minutes : {Timespan}")

Timespan = time.strftime("%S")
print(f"Seconds : {Timespan} ")